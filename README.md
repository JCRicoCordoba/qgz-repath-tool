# qgz-repath-tool
GUI tool to remap broken layer paths in QGIS projects (.qgz/.qgs) after moving to a new machine or restructuring folders. Auto-detects broken paths, groups them by common prefix and resolves them in bulk. PySide6 interface. No QGIS installation required.
